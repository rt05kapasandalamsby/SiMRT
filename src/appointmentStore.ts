/**
 * appointmentStore — DEPRECATED
 * Appointments are now managed directly in Firestore via JanjiTemu.tsx and WargaJanjiTemu.tsx
 */

export function countPendingJanji(): number {
  return 0;
}

export function loadAppointments(): any[] {
  return [];
}
